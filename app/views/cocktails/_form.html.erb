<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name, label: "Nom du cocktail" %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Sauvegarder", class:"btn btn-small btn-success" %>
<% end %>
